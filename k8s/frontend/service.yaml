apiVersion: v1
kind: Service
metadata:
  name: fifaapp-frontend-svc
  namespace: fifaapp
spec:
  selector:
    app: fifaapp-frontend
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
  type: NodePort